
# https://github.com/ibraheemdev/matchit

# host & path routing
routes:
  - route:
      type: host
      value: localhost
    name: app-route
    paths:
      - path:
          - /
          - /{*path}
        service:
          name: app-service